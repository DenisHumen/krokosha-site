# http-level settings (installed as /etc/nginx/conf.d/krokosha-http.conf).

# Access log as JSON lines: the API reads it incrementally for the «server traffic» dashboard,
# which also sees bots and visitors without JavaScript (brief B6).
log_format krokosha_json escape=json
    '{"time":"$time_iso8601","remote_addr":"$remote_addr","host":"$host","method":"$request_method",'
    '"uri":"$request_uri","protocol":"$server_protocol","status":$status,"bytes_sent":$bytes_sent,'
    '"request_time":$request_time,"referer":"$http_referer","user_agent":"$http_user_agent"}';

# Request rate per visitor address for the API. Generous for people (a page sends a handful of
# analytics batches), tight for scripts; the API has finer limits of its own per action.
limit_req_zone $binary_remote_addr zone=krokosha_api:10m rate=10r/s;

# The admin area: one person clicking. Its login form gets a much tighter limit — guessing
# passwords must be slow, and each check is deliberately expensive (argon2id). The API counts
# failures per login and per network on top of that, and fail2ban bans repeat offenders.
limit_req_zone $binary_remote_addr zone=krokosha_admin:1m rate=10r/s;
limit_req_zone $binary_remote_addr zone=krokosha_login:1m rate=20r/m;
