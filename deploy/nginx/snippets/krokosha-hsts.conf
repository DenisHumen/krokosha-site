# Only with a real certificate: HSTS together with a self-signed one locks browsers out.
# No includeSubDomains — other host names of the domain may still be served over plain HTTP.
add_header Strict-Transport-Security "max-age=31536000" always;
