# refs/ — визуальные референсы (ВРЕМЕННО)

> ⚠️ **Папка временная.** После того как дизайн сайта написан и утверждён Денисом, `docs/brief/refs/` **удаляется из репозитория** за ненадобностью.
> Файлы из `design/` и `web/` не должны ссылаться на эти файлы: после удаления такие ссылки сломаются.
>
> Обычный коммит с удалением убирает файлы из рабочей версии, но они остаются в истории git. Если нужно убрать их и из истории — `git filter-repo --path docs/brief/refs --invert-paths` + force-push (делается осознанно, отдельно).

Материалы ниже принадлежат их правообладателям. Они лежат здесь только как **вдохновение для дизайна** этого сайта — не для копирования и не для повторного использования. Не переносить на сайт чужие логотипы, тексты, названия брендов и точную вёрстку. Брать приёмы, ритм и настроение.

| Файл | Источник | Что берём (подробно — бриф A7–A10) |
|---|---|---|
| `ref-01-hero-hands.jpeg` | концепт лендинга «Axial.ai» | композиция hero, руки из символов, двухтоновый заголовок, нижние подписи, пилюли-кнопки |
| `ref-02-dotmatrix-nodes.jpeg` | сайт PlayerZero | узел → линии → пилюли (дерево навыков), dot-matrix цифры, разреженная символьная графика, двухтоновые заголовки |
| `ref-03-admin-dashboard.jpeg` | приложение Rize | стилистика админки (B6) |
| `ref-04-scroll-animation.mp4` | Aristide Benoist, «The Secret Project» | механика скролла: липкие выталкивающие номера, тёмный занавес, каркас с навигацией (A9) |
| `ref-04-scroll-frames-dark-panel.png` | раскадровка того же видео | тёмная панель-занавес |
| `ref-04-scroll-frames-sticky-numbers.png` | раскадровка того же видео | липкие номера 01 → 02 → 03 |
