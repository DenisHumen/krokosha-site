# Архитектура

Требования — в [brief/MASTER_PROMPT.md](brief/MASTER_PROMPT.md). Здесь — принятые решения и почему. Новые решения добавляются в журнал внизу.

## 1. Один публичный репозиторий

Бриф (0.1) допускает один репозиторий или пару публичный + приватный. Выбран **один публичный** `DenisHumen/krokosha-site`.

- **Витрина.** Для инженера инфраструктуры открытые `install.sh`, конфиги Nginx/systemd и Go API — само по себе портфолио. Репозиторий попадёт в собственный блок «Проекты».
- **Проще эксплуатация.** Одна копия на VPS, не нужен deploy key, один CI. Изменение дизайна и интеграции проходит одним PR.
- **Защита веток.** На бесплатном плане GitHub защита веток (main только через PR + обязательный CI) работает только в публичных репозиториях.
- **Безопасность не зависит от закрытости кода.** Секреты лежат только в `/etc/krokosha/env` (600, root:krokosha), в репозитории — `deploy/env/.env.example`. Путь админки задаётся при установке. Пароли хранятся в argon2id, есть rate-limit и fail2ban.
- **Цена решения.** Claude Design видит код бэкенда (безвредно). Эвристики антиспама открыты, но основная защита — ALTCHA и rate-limit, которые от этого не слабеют.

Если позже понадобится закрыть бэкенд: `api/` и `deploy/` выносятся в приватный `krokosha-infra`, названия каталогов не меняются (C1).

## 2. Стек

| Слой | Выбор |
|---|---|
| Сайт | Astro + TypeScript → статический HTML |
| Анимации | GSAP + ScrollTrigger, Lenis, Canvas/WebGL, Lottie/Rive |
| API | Go, один бинарник, systemd, `127.0.0.1:8080` |
| БД | **MySQL 8.4** в Docker — основное хранилище. Решение Дениса от 2026-09-18, заменяет SQLite из брифа (B1) |
| Кэш и быстрые данные | **Redis** в Docker — лимиты запросов, «сейчас на сайте», живая лента, буфер событий, кэш отчётов. Решение Дениса от 2026-09-18 |
| Веб | Nginx, Certbot |
| Почта | docker-mailserver на том же VPS — приём и отправка. Решение Дениса от 2026-09-18 |
| Контейнеры | Docker: MySQL, Redis, почта. Сайт, API и сборка — без Docker, под systemd |

Что где хранится — §6, сколько это стоит памяти — §7.

## 3. Поток данных

```
GitHub API ──► krokosha-sync (Go, таймер 6 ч) ──► content/generated/github.json   репозитории + tier (contract.md §5)
                  • ETag + кэш: при ошибке GitHub            content/generated/avatar.*     аватар как есть, без конвертации
                    отдаётся прошлый ответ, stale=true
                                                                       │
content/*.yaml ────────────────────────────────────────────────────────┤
                                                                       ▼
                                          astro build (web/)  ─► releases/<ts> ─► check-dist ─► симлинк current
                                            • схемы YAML, языки, склонения
                                            • experience_years от career_start
                                            • аватар → AVIF/WebP (sharp)
                                            • SEO: hreflang, JSON-LD, sitemap.xml, CSP
```

Разделение обязанностей: **sync только ходит в GitHub**, всё остальное делает сборка. Так `web/` собирается без Go (CI, локальная разработка, превью дизайна): если `content/generated/github.json` нет, берётся снимок репозиториев из `mock/en/projects.json`.

Стаж считается при каждой сборке, а не кроном «раз в год». Сборка идёт каждые 6 часов, поэтому число обновится само в нужный день (B4). «Сегодня» берётся в часовом поясе владельца (`site.yaml → timezone`), а не сервера.

Формат `content/generated/github.json` — часть `projects.json` из контракта: `{ "public": [...], "syncedAt": "…", "stale": false }`. Схема — `web/src/lib/schema.ts → githubDataSchema`; сборка отклонит файл с неизвестным `tier` или ссылкой не `http(s)://`.

## 4. Каталоги

См. README каждой зоны: [design](../design/README.md), [web](../web/README.md), [content](../content/README.md), [mock](../mock/README.md), [api](../api/README.md), [deploy](../deploy/README.md).

## 5. Сервер и ограничения установщика

Проверено на VPS 2026-09-18.

| Параметр | Значение | Следствие |
|---|---|---|
| ОС | Ubuntu 24.04.5 LTS, KVM | целевая ОС `install.sh` |
| CPU / RAM / swap | 2 vCPU / 1.9 ГБ / 2 ГБ | Astro-сборка — с `NODE_OPTIONS=--max-old-space-size`; почта — docker-mailserver **без ClamAV**, Rspamd включён; webmail (Q9) — нет, Mailcow не влезет |
| Диск | 28 ГБ, свободно 21 ГБ | 3 релиза сайта + почта + логи 30 дней помещаются; ротация обязательна |
| Сеть | 2 IPv4 на `eth0`, IPv6 нет | AAAA-записи не публикуем; nginx и почта слушают основной IPv4 |
| Исходящий 25/tcp | открыт | прямая доставка почты без relay |
| PTR | не выставлен | Выставляется **у хостера VPS, а не в DNS домена**: обратная зона `147.175.46.in-addr.arpa` обслуживается `ns1–ns4.s-host.com.ua`. Нужно значение `mail.krokosha.xyz` — через кабинет S-Host или тикет, до запуска почты |
| DNS домена | NIC.UA (`ns10–ns12.uadns.com`). Есть: A для `@`, `www`, `mail` → IP сервера; MX 10 `mail.krokosha.xyz`; лишний шаблонный CNAME `ftp` | SPF, DKIM, DMARC и autoconfig добавляются при установке почты — список выведет `install.sh` |
| Уже работает | WireGuard `wg0` (UDP 51820), `ip_forward=1`, NAT для VPN-подсети | **установщик не должен это ломать** (см. ниже) |
| SSH | вход только по ключу, root по SSH запрещён — `/etc/ssh/sshd_config.d/10-krokosha-hardening.conf` | до этого было ~22 тыс. попыток подбора пароля в сутки; `install.sh` поддерживает этот файл идемпотентно |

Правила для `install.sh`, вытекающие из этого:
- Не трогает сторонние сервисы. Дополнительные входящие порты передаются параметром (на этом VPS — `--allow 51820/udp`), UFW включается только после того, как все они добавлены.
- Не выключает `net.ipv4.ip_forward` и не сбрасывает существующие правила nftables/NAT. Для UFW: `DEFAULT_FORWARD_POLICY` и маршрутизация VPN-подсети сохраняются.
- Перед включением UFW проверяет, что правило для SSH уже есть, чтобы не отрезать доступ.

## 6. Данные: одна директория, чтобы легко переехать

Требование Дениса (2026-09-18): всё, что нельзя потерять, лежит **в одной отдельной директории**, чтобы переезд на другой сервер сводился к её копированию.

Корень данных — `/srv/krokosha` (меняется при установке: `install.sh --data-dir …`):

```
/srv/krokosha/
├── mysql/          каталог данных контейнера MySQL
├── redis/          AOF-журнал Redis
├── mail/           docker-mailserver: data/ (письма), state/, logs/, config/ (ящики, псевдонимы, DKIM-ключи), DNS.txt
├── attachments/    вложения заявок (600, отдаются только через админку)
├── backups/        ночные копии: дамп базы, настройки, сертификаты, письма и файлы (жёсткими ссылками между копиями), ротация
└── config/env      настройки и секреты; /etc/krokosha/env — ссылка на этот файл
```

Вне корня данных — только то, что восстанавливается само: код (`/opt/krokosha`, клон репозитория), релизы сайта (`/var/www/krokosha`, пересобираются), кэши (`/var/lib/krokosha`), сертификаты (перевыпускаются), база GeoIP (`/var/lib/GeoIP`, скачивается снова: DB-IP — сама, GeoLite2 — по ключу из настроек).

**Переезд** — через резервную копию, а не копированием живого каталога (внутри него — файлы работающего MySQL): `backup.sh` на старом сервере → обычная установка на новом с тем же доменом → `restore.sh` → переключить DNS и PTR → `install.sh --from-env --tls letsencrypt`. Пошагово — [deploy/README.md](../deploy/README.md), «Переезд на другой сервер».

**Кто за что отвечает:**

| | MySQL — источник истины | Redis — быстрое и временное |
|---|---|---|
| Аналитика | просмотры страниц, события, дневные соли | границы визита (30 минут тишины), счётчик «сейчас на сайте», лимит запросов с адреса. Входящие события копятся в памяти процесса и раз в секунду одной транзакцией пишутся в MySQL |
| Админка | пользователи, сессии входа, журнал действий | ограничение попыток входа, кэш тяжёлых отчётов |
| Заявки | заявки, переписка, заметки, шаблоны, **outbox** (очередь доставки — только в MySQL, в одной транзакции с заявкой) | лимит заявок с одного адреса, одноразовые ALTCHA-задачи |
| Бот | пользователи бота, приглашения, привязки сообщений | состояние диалога («жду текст ответа») |

Правило: **потеря Redis не теряет данных.** Если Redis недоступен, API продолжает работать с лимитами в памяти процесса, а события пишет в MySQL напрямую. MySQL и Redis слушают только `127.0.0.1`.

## 7. Память: что помещается в 2 ГБ

VPS — 1.9 ГБ RAM + 2 ГБ swap. Оценка; строки с пометкой «замер» проверены на локальном стенде (2026-09-18, пустая база):

| Компонент | RAM |
|---|---|
| ОС, sshd, fail2ban, WireGuard | ~200 МБ |
| nginx | ~20 МБ |
| krokosha-api | ~50 МБ |
| MySQL 8.4, настроенный на малую память (`deploy/compose/mysql/krokosha.cnf`: `innodb_buffer_pool_size=128M`, `performance_schema=OFF`, без binlog) | замер: 132 МБ на пустой базе; с данными до ~250 МБ. Лимит контейнера — 512 МБ |
| Redis (`maxmemory 48mb`) | замер: 4 МБ; до ~60 МБ. Лимит контейнера — 96 МБ |
| Docker (dockerd, containerd) | ~100 МБ |
| docker-mailserver: Postfix, Dovecot, Rspamd, без ClamAV | ~400–500 МБ |
| **Итого в покое** | **~1.0–1.2 ГБ** |
| Сборка сайта (раз в 6 часов, около минуты) | +0.5–0.8 ГБ — на это время уходит в swap |

Вывод: помещается, но впритык; контейнерам заданы жёсткие лимиты памяти, сборка ограничена `MemoryMax`. Когда заработает почта, разумно поднять VPS до 4 ГБ. Если памяти не хватит раньше — MariaDB вместо MySQL экономит 150–200 МБ, SQL в проекте пишется совместимым с обеими.

## 8. Где и как проверяем

Решение Дениса (2026-09-18): **на VPS ничего не выкладываем, пока бэкенд не реализован полностью**; проверяем локально в Docker. Дизайн — после бэкенда.

- **Локальный стенд** — `deploy/docker/staging/staging.sh up`: контейнер Ubuntu 24.04 с systemd, в котором отрабатывает настоящий `install.sh`. Сайт — `https://krokosha.localhost` (самоподписанный сертификат). Это копия VPS: nginx, UFW, fail2ban, таймеры, позже — MySQL, Redis и почта.
- **CI** — то же самое на одноразовой VM GitHub при каждом PR (`deploy/ci/test-install.sh`).
- **Почта локально** — отправка в тестовый SMTP-приёмник; настоящая доставка (SPF, DKIM, DMARC, попадание во «Входящие») проверяется только на VPS с реальным DNS. Для этого Денис разрешил использовать его Gmail через браузер: тестовые письма на адреса домена и обратно.
- **Порядок работ** — [roadmap.md](roadmap.md).

## 9. Журнал решений

| Дата | Решение | Кто |
|---|---|---|
| 2026-09-18 | Название `krokosha-site`, один публичный репозиторий | Денис одобрил |
| 2026-09-18 | Сервер — VPS, IP в DNS меняется на новый | Денис |
| 2026-09-18 | Показывать **все** публичные репозитории, проработанные — заметнее. Автоматический `tier` по метрикам GitHub, формула в contract.md §5 | Денис (идея), Backend (формула) |
| 2026-09-18 | Архивные репозитории показываются с меткой «архив» (бриф B3 их исключал — перекрыто решением выше) | Backend |
| 2026-09-18 | Ссылки homepage на t.me/github.com не считаются сайтом проекта | Backend |
| 2026-09-18 | Референсы лежат в `docs/brief/refs/` **временно**: после утверждения дизайна удалить | Денис |
| 2026-09-18 | Контракт v1.1: `tier`, `archived`, `stale`, `site.json`, `data-field`, секции `stats`/`curtain`, треки `cta-discuss`/`form-submit`/`projects-show-all` | Backend |
| 2026-09-18 | **Языки (Q1):** en — основной, uk, ru | Денис |
| 2026-09-18 | Адреса: en в корне `/`, `/uk/`, `/ru/` (без префикса для основного языка). `hreflang`: `en` + `x-default` → `/`, `uk`, `ru`. Код украинского — `uk` (ISO 639-1), кнопка переключателя — «UA» | Backend |
| 2026-09-18 | Контракт v1.2: `mock/` по локалям, строки `{ en, uk, ru }` в `content/`, plurals по CLDR, UI-строки формы/404/заголовков | Backend |
| 2026-09-18 | Рынок — Украина + англоязычные клиенты → Yandex не таргетируется (бриф B7). Поисковики: Google, Bing (через него DuckDuckGo и др.) | Backend |
| 2026-09-18 | **Telegram на сайте (Q3):** `https://t.me/DenisHumen` | Денис |
| 2026-09-18 | **Почта (Q3/Q7):** поднимаем свой почтовый сервер, публичный адрес на сайте — `denis@krokosha.xyz`. Служебный `leads@krokosha.xyz` для ответов клиентов создаётся установщиком (B10.5). Ответы клиентам по умолчанию уходят с `denis@` (Q12, подтвердить) | Денис / Backend |
| 2026-09-18 | **Начало карьеры (Q2):** `career_start: 2017-09-18`. Стаж = полные годы; 2026-09-18 — годовщина, стаж стал 9 (в брифе «8» — на момент написания). Граничные даты — для unit-тестов B4: 2026-09-17 → 8, 2026-09-18 → 9 | Денис |
| 2026-09-18 | **VPS (Q5):** хостер S-Host (сеть NETH LLC, площадка в Нидерландах), Ubuntu 24.04 (OpenSSH 9.6p1). DNS `@`, `www`, `mail` уже указывают на него. Доступ агента — только по SSH-ключу, без паролей | Денис / Backend |
| 2026-09-18 | На VPS вход по паролю и root-вход по SSH отключены (key-only). Параметры сервера и ограничения установщика — §5 | Денис одобрил |
| 2026-09-18 | **Версии:** Astro 7, TypeScript 6 (не 7: `astro check` и typescript-eslint его ещё не поддерживают), Node.js ≥ 22.12, в CI и на сервере — 24 LTS. Зависимости закреплены точными версиями, обновления приходят PR-ами от Dependabot | Backend |
| 2026-09-18 | **Стаж, языки и склонения считает сборка (TypeScript), а не Go-sync** (в брифе B9 тест стажа значился за Go). Причина: `web/` должен собираться без Go. Граничные даты B4 покрыты unit-тестами Vitest | Backend |
| 2026-09-18 | **`mock/` генерируется тем же кодом, что и страницы** (`web/src/lib/data.ts`, `npm run mock`), CI проверяет актуальность. Контракт с дизайном не может разойтись с реальными данными | Backend |
| 2026-09-18 | **Аватар конвертирует сборка (sharp в Astro), sync только скачивает файл.** В Go нет надёжного кодировщика AVIF без cgo. В репозитории лежит только нейтральная заглушка: сам аватар в публичный репозиторий не коммитится, на сервере его скачивает sync. Приоритет: `content/avatar.*` → `content/generated/avatar.*` → заглушка | Backend |
| 2026-09-18 | **CSP:** `security.csp` в Astro — хэши своих скриптов и стилей в `<meta>`; директивы, которые в `<meta>` не работают (`frame-ancestors`), добавит Nginx. В разметке нет inline-обработчиков и атрибутов `style` | Backend |
| 2026-09-18 | **`sitemap.xml` и `robots.txt` — свои эндпоинты**, без `@astrojs/sitemap`: страниц мало, зато точное имя файла, hreflang-альтернативы и `lastmod` по дате последнего push. Служебные страницы (404, `/play`, черновики) — `noindex` и вне карты | Backend |
| 2026-09-18 | **Форма заявки не выводится, пока нет API заявок** — чтобы на живом сайте не терять обращения. До этого в «Контактах» — кнопки Telegram / почта / GitHub. *Выполнено 2026-09-19: форма выводится, API заявок работает (шаг 5a)* | Backend |
| 2026-09-18 | **CI — обязательная проверка для `main`:** формат, ESLint (+ доступность), типы, unit-тесты, актуальность `mock/`, сборка, проверка `dist/`. Экшены закреплены по SHA | Backend |
| 2026-09-18 | **E2E и Lighthouse в CI (B7, B9):** Playwright по собранному сайту (десктоп + телефон 360 px): ошибки консоли, нарушения CSP, WCAG 2.1 AA через axe в обеих темах, языки, тема до первой отрисовки. Lighthouse CI: все категории ≥ 95, LCP < 2.5 с, CLS < 0.1, TBT < 200 мс — иначе PR не вливается | Backend |
| 2026-09-18 | **GitHub sync — REST, без GraphQL.** GraphQL требует токен всегда, а сайт должен обновляться и без него. Метрики репозитория кэшируются до изменения `pushed_at`/`updated_at`, поэтому обычный запуск — один запрос, и лимита 60 запросов/час хватает | Backend |
| 2026-09-18 | **`stale: true` — только после 24 часов без ответа GitHub.** Один неудачный запуск (таймер каждые 6 ч) не повод показывать посетителям предупреждение | Backend |
| 2026-09-18 | **Ранжирование отделено от сети:** `pinned` / `exclude` / `overrides` применяются к кэшу при каждом запуске, даже когда GitHub недоступен. Переведённое описание из `overrides` подставляет сборка сайта (в `github.json` языков нет) | Backend |
| 2026-09-18 | **Цвета языков** — таблица из GitHub Linguist (MIT), встроена в бинарник; обновляется `go generate`. REST API цвет не отдаёт | Backend |
| 2026-09-18 | **Go:** минимум зависимостей (сейчас одна — YAML), стандартная библиотека для HTTP/CLI/логов. CI: golangci-lint, тесты с детектором гонок, статическая сборка `CGO_ENABLED=0` | Backend |
| 2026-09-18 | **Вендоры (частично Q4/навыки):** сети — Cisco (Catalyst, Nexus, ISR/ASR), Arista, Juniper, Extreme Networks, Fortinet, MikroTik, Ubiquiti; серверы — Dell (Dell EMC), HPE, Supermicro, Fujitsu. То, что уже было на сайте, остаётся | Денис |
| 2026-09-18 | **Go и Node.js на сервере — из официальных архивов** с закреплёнными версиями и SHA-256 (`deploy/toolchain.env`), в `/opt/krokosha/toolchain`. Без сторонних apt-репозиториев и без установки в систему: воспроизводимо, удаляется одной командой. Бинарники Go собираются на сервере — `update.sh` не зависит от выпуска релизов | Backend |
| 2026-09-18 | **Сборка не видит секретов:** npm и Astro запускаются с чистым окружением, юнит `krokosha-sync` изолирован systemd (`ProtectSystem=strict`, без привилегий, лимит памяти). Сторонний код из npm не должен иметь доступа к токенам и паролям из `/etc/krokosha/env` | Backend |
| 2026-09-18 | **`update.sh` = `git pull` + тот же `install.sh --from-env`.** Один идемпотентный путь вместо двух похожих скриптов; выбор оператора (`--skip-firewall`, `--skip-dns-check`, `--allow`) запоминается и при обновлениях соблюдается | Backend |
| 2026-09-18 | **Согласие с условиями Let's Encrypt — только явное** (вопрос при установке или `--agree-tos`); скрипт не принимает соглашения молча | Backend |
| 2026-09-18 | **UFW и существующий WireGuard:** интерфейсы определяются автоматически, порт и `ufw route allow in on <wg>` добавляются до включения фаервола; `ip_forward` и NAT не трогаются. Проверяется в CI на настоящей VM с тестовым WireGuard-интерфейсом | Backend |
| 2026-09-18 | **HSTS без `includeSubDomains`** и только с настоящим сертификатом: на поддоменах может жить что-то по HTTP, а самоподписанный сертификат вместе с HSTS запирает браузер | Backend |
| 2026-09-18 | **Установщик проверяется в CI целиком:** установка → повторный запуск → `update.sh` → откат → удаление на одноразовой Ubuntu 24.04 (обязательная проверка для `main`) | Backend |
| 2026-09-18 | **Хранилище: MySQL + Redis** вместо SQLite из брифа. Оба — в Docker, слушают только localhost, настроены на малую память (§7). Роли — §6 | Денис |
| 2026-09-18 | **Все данные — в одной директории** (`/srv/krokosha`: MySQL, Redis, почта, вложения, дампы, настройки), чтобы переезд сводился к копированию (§6) | Денис |
| 2026-09-18 | **Почта — на том же VPS в Docker**, приём и отправка, данные в той же директории | Денис |
| 2026-09-18 | **На VPS не выкладываем, пока бэкенд не готов полностью.** Проверка — локально в Docker (стенд с настоящим установщиком). Дизайн — после бэкенда. Вопрос о согласии с Let's Encrypt отложен до выкладки | Денис |
| 2026-09-18 | **Живые тесты почты — через Gmail Дениса в его браузере** (разрешено им), когда сервер будет в сети | Денис |
| 2026-09-18 | **Outbox — только в MySQL**, в одной транзакции с заявкой; Redis для очередей доставки не используется: заявка не должна зависеть от хранилища без гарантий | Backend |
| 2026-09-18 | **Redis необязателен для работы:** при его недоступности API переходит на лимиты в памяти и пишет события в MySQL напрямую | Backend |
| 2026-09-18 | **Аналитика: очередь записи — в памяти процесса, не в Redis.** Раз в секунду одна транзакция в MySQL; при остановке сервиса очередь дописывается. Потеря секунды статистики при аварии не стоит отдельного механизма | Backend |
| 2026-09-18 | **В Redis нет IP-адресов:** ключи лимитов — солёный хэш адреса. Проверяется тестом, который ищет адрес и User-Agent во всех таблицах и ключах | Backend |
| 2026-09-18 | **«Секция на экране»** = пересекает среднюю треть окна или видна целиком. Правило «видно 40% секции» не работает для секций выше экрана (список проектов) — поймано e2e-тестом | Backend |
| 2026-09-18 | **Гео по IP отложено** до шага «Эксплуатация»: нужна локальная база (GeoLite2 требует ключ MaxMind от Дениса; альтернатива — DB-IP Lite с обязательной ссылкой на сайте). Колонки `country`/`city` уже есть | Backend |
| 2026-09-18 | **Админка — серверные страницы в бинарнике API** (`html/template`, шаблоны и CSS встроены через `embed`), без фронтенд-сборки и без inline-кода: CSP `default-src 'none'; script-src 'self'; style-src 'self'`, графики — SVG с сервера. Стиль — по ref-03, классы стабильные: Claude Design может перекрасить позже | Backend |
| 2026-09-18 | **Сессии админки — непрозрачный токен, в базе только SHA-256.** Не JWT: сеанс должен завершаться мгновенно (выход, смена пароля, кнопка «завершить»). Cookie `__Host-ks`: браузер сам гарантирует `Secure`, `Path=/` и отсутствие `Domain`. 2 часа без активности, не дольше суток | Backend |
| 2026-09-18 | **argon2id: 64 МБ × 3 прохода, не больше двух хэшей одновременно.** На VPS с 2 ГБ подбор паролей не должен выесть память; несуществующий логин проверяется по фиктивному хэшу — время ответа не выдаёт, какие логины настоящие | Backend |
| 2026-09-18 | **TOTP: принятый шаг записывается в базу** (`UPDATE … WHERE totp_last_step < ?`) — подсмотренный код повторно не сработает даже при двух одновременных запросах. QR-код рисует сервер. Вход в два шага через одноразовый билет в кэше: пароль в HTML не возвращается | Backend |
| 2026-09-18 | **Админка только по HTTPS и в отдельном логе nginx.** С `--tls none` она не проксируется вовсе (cookie `Secure`, пароль не должен идти открытым текстом). Отдельный `nginx-admin.json.log`: секретный путь и клики владельца не попадают в статистику трафика, а fail2ban считает неудачные входы по нему (401/429) — полный IP API по-прежнему нигде не хранит | Backend |
| 2026-09-18 | **Графики админки рисует сервер (SVG), живая лента — SSE.** Ни одной графической библиотеки и ни одного inline-стиля: CSP админки остаётся `default-src 'none'`, страница работает без JavaScript (кроме ленты). SSE, а не WebSocket: поток в одну сторону, обычный HTTP через nginx (`X-Accel-Buffering: no`), переподключение — силами браузера | Backend |
| 2026-09-18 | **Открытая вкладка админки не продлевает сеанс:** живая лента проверяет сеанс без отметки об активности, закрывается при выходе или отзыве и раз в 30 минут переустанавливается. Забытый на экране дашборд разлогинится через 2 часа, как и положено | Backend |
| 2026-09-18 | **Отчёты считаются запросами к сырым событиям**, без промежуточных агрегатов: у сайта-визитки это тысячи строк в месяц. Дневные агрегаты появятся вместе с очисткой сырых данных (шаг 8). Группировка по часам — в Go, в часовом поясе владельца: без `CONVERT_TZ` и таблиц часовых поясов в MySQL, переход на летнее время покрыт тестами | Backend |
| 2026-09-18 | **Действие = клик по размеченному элементу (`data-track`).** Клик по внешней кнопке даёт два события (`click` и `outbound`), поэтому внешние переходы показываются отдельным списком и в счётчик действий не входят | Backend |
| 2026-09-18 | **Визиты владельца не попадают в статистику:** запрос к `/api/e` с cookie админки (пусть даже просроченной) принимается и отбрасывается | Backend |
| 2026-09-18 | **CSV-экспорт обезвреживает формулы** (`=`, `+`, `-`, `@` в начале ячейки получают апостроф): UTM-метки и цели кликов — чужой ввод, а файл открывает владелец в табличном редакторе | Backend |
| 2026-09-19 | **Форма работает без JavaScript, страница «спасибо» — из релиза.** API не рисует HTML сам: он берёт собранную Astro страницу `/thanks/` и подставляет номер заявки в метки `%%…%%`. Дизайн остаётся за `web/`, а посетитель без скриптов видит ту же страницу, что и все. Ответ на POST — редирект (PRG): перезагрузка не отправляет форму повторно | Backend |
| 2026-09-19 | **Ошибки без JavaScript показывает CSS `:target`:** API возвращает на `/<язык>/#form-error-…`, блок с этим `id` есть в статической странице. Ни серверного рендера формы, ни cookie для flash-сообщений | Backend |
| 2026-09-19 | **Спам не отклоняется, а сохраняется молча.** Отправитель видит обычное «спасибо» и не узнаёт, какое правило сработало; ошибка фильтра исправляется одним кликом в админке, а не потерянным клиентом. Решение — по баллам, без JavaScript (а значит, без proof-of-work) заявка проходит, если текст не подозрителен | Backend |
| 2026-09-19 | **Proof-of-work — свой, совместимый с ALTCHA по протоколу,** без их виджета (30 КБ): ~40 строк в `form.js` на `crypto.subtle`. Соль обязана кончаться разделителем: иначе решённую задачу можно «перекроить», перенеся цифры из числа в соль, и продлить ей срок. Одно решение — одна заявка | Backend |
| 2026-09-19 | **`APP_SECRET`** — один секрет установки для всего, что подписывается: задачи proof-of-work, ключи лимитов (HMAC адреса вместо адреса), позже — адреса ответов `leads+K-0042.<подпись>@`. Генерируется установщиком, хранится в `/etc/krokosha/env` | Backend |
| 2026-09-19 | **Outbox ждёт ненастроенный канал:** пока нет SMTP или токена бота, задачи лежат `pending` и не тратят попытки. Заявки, пришедшие до настройки почты, уведомят сразу после неё | Backend |
| 2026-09-19 | **Письмо собирается при отправке, а не при постановке в очередь:** в задаче только номер заявки. Задача, пережившая сбой почты, не отправит устаревшие данные; удалённая по запросу клиента заявка не уйдёт письмом | Backend |
| 2026-09-19 | **Лог nginx узнаётся по отпечатку первой строки**, а не по inode: переживает перезапуск сервиса, ротацию, `copytruncate` и переезд, одинаково работает на любой ОС. Позиция чтения сохраняется в одной транзакции с суммами — запрос нельзя ни потерять, ни посчитать дважды | Backend |
| 2026-09-19 | **Из лога хранятся только суммы.** Адреса — усечённые и только у сканеров, User-Agent — только семейство («Googlebot», «Chrome»), путь — без query-строки. Число разных адресов страниц ограничено 2000 в день: перебор адресов сканером складывается в одну строку | Backend |
| 2026-09-19 | **Время ответа — гистограмма по минутам**, а не готовые перцентили: p50 и p95 за любой период получаются сложением корзин, перцентили же складывать нельзя | Backend |
| 2026-09-19 | **Разные клиенты за час — HyperLogLog в Redis** (12 КБ на счётчик, в Redis остаются регистры оценки, а не адреса), без Redis — множество в памяти. В MySQL попадает только число | Backend |
| 2026-09-19 | **Кнопка «пересобрать» — файл-запрос + systemd path-юнит.** Веб-сервис, доступный из интернета, не получает ни sudo, ни polkit, ни D-Bus: единственное, что он может, — создать файл в одном каталоге. Сборка идёт тем же юнитом и в той же песочнице, что и по таймеру | Backend |
| 2026-09-19 | **Сертификат для экрана статуса читается по TLS у собственного nginx**, а не из `/etc/letsencrypt`: у сервиса нет туда доступа, и важно именно то, что видят посетители. Ответ кэшируется на 5 минут | Backend |
| 2026-09-19 | **Экран статуса не падает, когда что-то сломано:** каждая часть читается отдельно и при ошибке описывается словами. Сборка пишет отчёт и при сбое (trap), с шагом, на котором остановилась | Backend |
| 2026-09-19 | **Одна модель заявок для админки и бота.** Экраны не пишут в таблицы сами: «взять», статус, ответ, заметка — методы `leads.Store`, и бот будет звать их же. «Взять» — это `UPDATE … WHERE status = 'new'`: блокировкой служит само условие, второй нажавший узнаёт имя первого, где бы он ни нажал | Backend |
| 2026-09-19 | **Ответ клиенту — сообщение в базе + задача в outbox в одной транзакции.** Экран отвечает сразу, доставка идёт с повторами; у сообщения есть состояние `queued → sent / failed`, видимое в ленте. Message-ID писем детерминированы (`lead-42.reply-7@домен`), цепочка собирается через `References`: повтор доставки не плодит писем, переписка у клиента — одной веткой | Backend |
| 2026-09-19 | **Клиенту, оставившему телефон, «ответ» не отправляется:** текст сохраняется как итог звонка. Система не делает вид, что умеет звонить, но лента заявки остаётся полной, а «время первой реакции» считается честно | Backend |
| 2026-09-19 | **Удаление по запросу клиента — физическое и целиком:** заявка, переписка, заметки, история, неотправленные задачи outbox — одной транзакцией. Остаётся строка журнала «K-0042 удалена, кем, когда». Подтверждение — номер, набранный руками: действие необратимо | Backend |
| 2026-09-19 | **Стартовые шаблоны ответов кладёт миграция, и только в пустую таблицу.** Дальше шаблоны — данные владельца: ни обновление, ни повторная установка их не перезаписывают | Backend |
| 2026-09-19 | **Доска заявок — прогрессивное улучшение.** Перетаскивание — ~80 строк без библиотек поверх тех же POST-форм; правила переходов знает только сервер, скрипт лишь показывает его ответ. CSP админки остаётся без `unsafe-inline` | Backend |
| 2026-09-19 | **Тип вложения определяется по содержимому, имя — только заявление отправителя.** Расширение выбирает, что ожидать; сигнатура и структура файла должны это подтвердить. docx проверяется по оглавлению zip без распаковки. Файл на диске называется случайно, присланное имя живёт в базе и нигде не становится путём | Backend |
| 2026-09-19 | **Вложения отдаются только как загрузка:** `attachment`, `octet-stream`, `nosniff`, `CSP: sandbox` — админка никогда не показывает и не исполняет присланное. Отдельного «просмотра картинок» нет намеренно | Backend |
| 2026-09-19 | **Файл пишется на диск последним** — после лимита, proof-of-work и антиспама: отказы ничего не оставляют, а файлы роботов не сохраняются вообще. Файлы без строки в базе (оборванный запрос, неудачное удаление) убирает ежедневная уборка; поэтому удаление заявки не может «застрять» из-за файла | Backend |
| 2026-09-19 | **Большие запросы nginx пропускает только на `= /api/leads`** (32 МБ, свой лимит частоты и одновременных загрузок), остальной API остаётся на 64 КБ. Потолок в nginx постоянный, а не зависит от флага в `content/site.yaml`: содержимое обновляется без root, конфигурация nginx — нет. Точные пределы держит API: пока вложения выключены, он читает 48 КБ | Backend |
| 2026-09-19 | **По истечении срока заявка обезличивается, а не удаляется** (по умолчанию): воронка и источники за прошлые годы остаются верными, а человека в них уже нет. Срок считается от последней активности, а не от создания: переписка, которая идёт, не исчезает посреди разговора. Спам живёт 30 дней | Backend |
| 2026-09-19 | **Bot API — своими руками, без библиотеки:** десяток методов поверх `net/http`. Главная причина — токен: он стоит в адресе каждого запроса, а ошибки `net/http` цитируют адрес. Свой клиент переписывает каждую ошибку, и токен не может оказаться в журнале systemd | Backend |
| 2026-09-19 | **Секреты webhook выводятся из `APP_SECRET`** (HMAC с разными метками: конец адреса и заголовок). Ни настраивать, ни хранить, ни синхронизировать с nginx нечего; смена `APP_SECRET` меняет оба, бот при старте сам сообщает Telegram новый адрес | Backend |
| 2026-09-19 | **Приглашение — единственный вход в бот, даже для владельца.** Первого владельца впускает код, который печатает установщик (или `krokosha-cli bot invite --owner`). Никаких «id владельца в конфиге»: id человек узнаёт только из самого Telegram, а ник — не идентификатор | Backend |
| 2026-09-19 | **Обновления обрабатываются по одному, через очередь.** Webhook отвечает Telegram сразу и кладёт обновление в очередь; один обработчик разбирает её по порядку. Порядок сообщений человека сохраняется, медленный ответ Telegram не вызывает повторных доставок, а очередь на 256 мест не даёт исчерпать память | Backend |
| 2026-09-19 | **nginx пускает на webhook только сети Telegram**, а адрес webhook не пишется в access-лог — иначе секрет из адреса оказался бы на экране «трафик сервера». Если Telegram когда-нибудь сменит сети, остаётся `TELEGRAM_MODE=polling` | Backend |
| 2026-09-19 | **Бот стирает за собой.** Всё, что он написал о заявке, записано в `bot_messages` (без внешнего ключа — строки должны пережить заявку). Удаление данных клиента или обезличивание заменяет эти сообщения строкой «данные удалены»: то, что стёрто на сервере, не остаётся жить в чатах Telegram. Сервер при этом Telegram не ждёт — стирание повторяется, пока не получится | Backend |
| 2026-09-19 | **Карточки синхронизируются подпиской на хранилище заявок** (`Store.OnChange`), а не вызовами из экранов админки: любое будущее место, меняющее заявку (почта, напоминания), обновит карточки само. Слушатель только кладёт номер в очередь; перерисовку делает отдельный обработчик, чтобы запрос админки не ждал Telegram | Backend |
| 2026-09-19 | **Третий исход доставки — «пока некому»** (`outbox.NotReady`): задача ждёт и не тратит попытки. Им пользуется бот, в который ещё никто не вошёл; вход первого человека делает ожидающие задачи срочными (`Worker.Hurry`) | Backend |
| 2026-09-19 | **Диалоги бота — одно поле JSON у человека со сроком жизни два часа**, а не таблица состояний: у человека один диалог за раз, и текст, набранный назавтра, не должен стать ответом на забытый вопрос. Отправка идёт только после предпросмотра и только один раз: черновик стирается вместе с диалогом | Backend |
| 2026-09-19 | **Ссылка клиента в бот — это токен его заявки** (тот же, что открывает страницу «спасибо»): отдельной таблицы одноразовых кодов нет. Одноразовость обеспечивает связь «одна заявка — один аккаунт Telegram» (первичный ключ), а не сгорание кода: тот же человек может открыть ссылку ещё раз — с другого устройства, после переустановки Telegram | Backend |
| 2026-09-19 | **Ответ идёт тем путём, которым клиент написал последним,** а поле формы «как связаться» — только до первого сообщения клиента. Так решает одно место (`leads.replyChannel`), и админка с ботом показывают одинаковую подсказку | Backend |
| 2026-09-19 | **В списках бота нет имён клиентов.** `/leads`, `/search`, дайджест не принадлежат одной заявке, и стереть их при удалении данных клиента нельзя; поэтому в них только номер, направление, дата и кто взял. Имя — на карточке, а она стирается | Backend |
| 2026-09-19 | **Напоминание о невзятой заявке — одно,** отметка о нём живёт в истории заявки (`lead_events`), без отдельной таблицы и без Redis: перезапуск сервиса не вызывает повторных напоминаний. Дайджест хранит «за какой день отправлен» в `app_meta` и пишет её до отправки: сбой посреди рассылки не превращается в дайджест каждую минуту | Backend |
| 2026-09-19 | **Почтовый сервер — профиль того же compose-файла**, а не отдельный проект: одна команда поднимает всё, `--no-mail` просто не включает профиль. Сертификат у почты тот же, что у сайта (имя `mail.` добавляется в него), — один выпуск, одно продление, один hook | Backend |
| 2026-09-19 | **Сайт входит на почтовый сервер как служебный ящик `leads@`,** а не как ящик владельца: пароль владельца не лежит в настройках сервиса, смена этого пароля ничего не ломает. Конверт писем — `leads@`, заголовок `From` — владелец: сервер разрешает учётной записи только собственный адрес, а возвраты приходят туда же, куда ответы клиентов | Backend |
| 2026-09-19 | **Ящики — файл с хэшами в каталоге данных,** который пишут установщик и `krokosha-mailbox`; пароль нигде не передаётся аргументом команды. Штатный `setup email add <адрес> <пароль>` показывал бы пароль всем процессам машины | Backend |
| 2026-09-19 | **Адрес для ответа подписан** (`leads+k-0042.<HMAC>@`): номер заявки в адресе ничего не стоит подобрать, подпись — нельзя. Почтовый сервер игнорирует всё после «+», сервис по нему находит заявку | Backend |
| 2026-09-19 | **Серые списки выключены, ClamAV не ставится.** Первый ответ клиента не должен ждать повторной доставки; антивирус занял бы больше памяти, чем всё остальное на сервере, а вложения сервис принимает только заведомо неисполняемых типов | Backend |
| 2026-09-20 | **IMAP — свой небольшой клиент, без библиотеки.** Сервису нужна дюжина команд к одному серверу, который ставит наш же установщик; готовые библиотеки — либо в вечной бете, либо тянут за собой больше, чем весь остальной сервис. Разбор письма — стандартная библиотека Go плюс `golang.org/x/text` для старых кодировок | Backend |
| 2026-09-20 | **Разобранное письмо из ящика удаляется, неразобранное — остаётся в нём.** Текст и файлы письма, нашедшего заявку, живут только в заявке и исчезают вместе с ней — удаление данных клиента не оставляет копии в почте. Письмо без заявки, наоборот, не распаковывается вовсе: в базе только то, что видно в списке, файлы чужого человека не касаются диска, пока администратор не скажет, чьё это письмо | Backend |
| 2026-09-20 | **Одно письмо — один раз:** журнал входящих хранит хэш `Message-ID`, а строка журнала пишется в одной транзакции с сообщением заявки. Письмо, доставленное дважды (адрес в `To` и в `Cc`), и остановка сервиса между базой и ящиком не создают дубликатов и не теряют писем | Backend |
| 2026-09-20 | **Отчёту о недоставке верим только по подписанному адресу** в возвращённых заголовках. Номер заявки в тексте может напечатать кто угодно; подпись адреса `Reply-To` знает только клиент этой заявки | Backend |
| 2026-09-20 | **Файлы не уходят в Telegram** ни из формы, ни из писем: бриф требует и «вложения — документами» (B10.5), и «отдаются только через авторизованную админку» (B10.7). Выбрано второе: файл, отправленный в чат, нельзя стереть при удалении данных клиента. В Telegram приходит «📎 файлов: N — в админке» | Backend |
| 2026-09-20 | **Неверный пароль почтового ящика пробуется один раз.** Сервис входит на почтовый сервер с той же машины; шесть неудач — и fail2ban почтового контейнера заблокировал бы его на неделю вместе с исходящей почтой сайта | Backend |
| 2026-09-18 | **Первый администратор создаётся установщиком**: пароль спрашивает сам `krokosha-cli` (без эха, дважды) либо берётся из файла (`--admin-password-file`, для CI и стенда). Пароль никогда не генерируется и не печатается молча на реальном сервере; в аргументах командной строки и переменных окружения его нет | Backend |
| 2026-09-22 | **Резервная копия — снимок с жёсткими ссылками** (`rsync --link-dest`), а не архив: месяц копий писем и файлов занимает место одной, любую можно открыть как каталог, восстановление — тот же `rsync` обратно. База — обычный `mysqldump` (файлы MySQL «на живую» копировать нельзя). Копия рядом с данными спасает от ошибки, не от диска: `BACKUP_RSYNC_TO` уносит каждую копию на другую машину | Backend |
| 2026-09-22 | **Восстановление — на установленный сайт, по белому списку настроек.** `restore.sh` требует сначала обычную установку и возвращает из копии базу, письма, файлы и только те настройки, которые обязаны остаться прежними (секрет подписи адресов и ссылок, путь админки, пароль служебного ящика, токен бота с адресом его Bot API, ключи MaxMind и IndexNow, сроки хранения). Пароли базы и Redis, пути, домен, способ получения сертификата — свои у каждой установки. Так копия с одного сервера безопасно ложится на другой | Backend |
| 2026-09-22 | **Слежение за сертификатами смотрит на то, что отдаётся**, а не на файлы: `openssl s_client` к nginx (`:443`) и почте (`:993`). Продлённый файл, который никто не перечитал, ничем не лучше просроченного. Меньше 14 дней — `certbot renew` сразу, неудача — оповещение владельцу | Backend |
| 2026-09-22 | **Оповещения о сервере идут через тот же outbox**, что и уведомления о заявках: `krokosha-cli alert` ставит письмо и сообщение в Telegram в очередь с повторами; скрипты ничего не отправляют сами и не знают, как. Один повод — одно оповещение в сутки (`--key`) | Backend |
| 2026-09-22 | **Дневные итоги статистики хранятся бессрочно, сырые события — 12 месяцев.** Суммы дня и по 50 строк на измерение считаются раз в 6 часов через час после полуночи владельца; сырые данные удаляются только за дни, итоги которых уже есть. Обзор старого периода строится из итогов и говорит об этом; часы, визиты и экспорт за него недоступны | Backend |
| 2026-09-22 | **Гео — GeoLite2 по ключу Дениса, а не DB-IP Lite:** лицензия DB-IP требует ссылку на их сайт на каждой странице, GeoLite2 — только бесплатный аккаунт. Ключ живёт в `/etc/krokosha/env`, установщик пишет `/etc/GeoIP.conf` и включает таймер `geoipupdate` дважды в неделю; сервис замечает новый файл сам. Спрашивается полный адрес, хранится ответ; без базы всё работает, страны «не определены» | Backend |
| 2026-09-22 | **IndexNow сообщает только об изменившихся страницах**, сравнивая HTML релиза с предыдущим байт в байт — сборка Astro воспроизводима (проверено: два сборки подряд идентичны), поэтому пересборка каждые 6 часов не превращается в спам. Ключ генерируется установщиком один раз и отдаётся сайтом как `/<ключ>.txt`; Google в IndexNow не участвует — ему sitemap и Search Console | Backend |
| 2026-09-22 | **Названия стран в админке — из CLDR** (`golang.org/x/text/language/display`, +3 МБ к бинарнику) вместо своего словаря: «Украина», «Германия» вместо кодов; неизвестный код показывается как есть. Города — как в базе, по-английски: язык названия зависит от базы, а не от нас | Backend |
| 2026-09-23 | **Гео — DB-IP City Lite по умолчанию, GeoLite2 по ключу — по желанию** (пересматривает запись от 2026-09-22): аккаунта MaxMind нет, и страны не определялись вовсе. Лицензия DB-IP (CC BY 4.0) просит ссылку на DB-IP на страницах, которые показывают или используют результаты. Результаты видны только в админке — «IP Geolocation by DB-IP» стоит внизу каждой её страницы; публичные страницы результатов не показывают. `krokosha-dbip.timer` раз в день смотрит, есть ли файл этого месяца (DB-IP выкладывает 1-го числа), и качает ~60 МБ раз в месяц; битый или чужой файл не заменяет рабочий. С ключом MaxMind установщик переводит `GEOIP_DB` на GeoLite2, как только её файл на месте, и убирает DB-IP; пока ключ не принёс файл, работает DB-IP | Backend |

## 10. Открытые вопросы

Отслеживаются как `TODO(Денис)` в `content/*.yaml`. Блокирующих нет. Перед выкладкой на VPS: согласие с условиями Let's Encrypt и адрес администратора; перед запуском почты — PTR `mail.krokosha.xyz` у S-Host (см. §5).

Не блокирующие: Q4 («Patch»), подтверждение навыков со `confirmed: false`, Q7 (нужны ли ещё ящики кроме `denis@` и служебного `leads@`), Q8 (реклама), Q9 (webmail), Q10 (бот), Q11 (напоминания), Q12 (адрес и подпись для ответов клиентам).
